# Annoto

## Development

Use **Trunk** to build the WebAssembly (WASM) module and `index.html`.

To start the development server, run the following command:

```bash
trunk serve
```

To build the release version, run:

```bash
trunk build --release
```
